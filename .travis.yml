language: ruby
cache: bundler

rvm:
  - 2.2.2

script: 'bundle exec rake'

notifications:
  email:
    recipients:
      - sebastian.schkudlara@vizzuality.com
    on_failure: change
    on_success: never

# addons:
#   code_climate:
#     repo_token: 

