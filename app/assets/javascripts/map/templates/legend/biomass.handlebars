<div class="layer-details layer-details-carbon_stocks">
  {{#if threshold}}
    <p class="canopy">Displaying above ground biomass with <span class="canopy-button"> > {{threshold}} %</span> canopy density.</p>
  {{/if}}
</div>