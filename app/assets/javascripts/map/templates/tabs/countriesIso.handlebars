{{!-- This should be better to be in another template,
but let's do that when it will be clear where it goes --}}
<div class="graph-line-bar is-hidden">
  <div id="TreeLoosCarbonEmissions-graph"></div>
  {{!-- <div class="graph-legend" id="#TreeLoosCarbonEmissions-graph-legend">
    <div class="tree-cover-loss">
      <p>gross-carbon-emissions</p>
      <span class="unit">Ha</span>
      <span class="tree-cover-loss-value">10.657</span>
    </div>
    <div class="gross-carbon-emissions">
      <p>Gross carbon emissions</p>
      <span class="unit">TCO2</span>
      <span class="gross-carbon-emissions-value">0,04765</span>
    </div>
  </div> --}}
</div>
{{#if layers}}
<header>
  <div class="alone">
    <p>Available data sets for this country:</p>
  </div>
</header>
<ul class="layers-list">
  {{#each layers}}
    <li class="layer" data-layer="{{this.slug}}" data-layeriso="{{this.iso}}">
      <span class="onoffswitch"><span></span></span>
      <span class="layer-title">{{this.title}} <a href='#' data-source='{{this.slug}}' class='source'><svg><use xlink:href="#shape-info"></use></svg></a></span>
    {{#if this.does_wrapper}}
      <span class="does_wrapper"></span>
    {{/if}}
    </li>
  {{/each}}
</ul>
{{else}}
<header>
  {{!-- <div class="alone">
    <p>No country data is available yet. </p>
    <p><a href="#" class="source" data-source="view_existing_global_data">View existing global data sets</a> or <a href="/getinvolved/share-data" target="_blank">suggest your own.</a></p>
  </div> --}}
  <h2>Select a country  <br>to analyze</h2>
</header>
{{/if}}