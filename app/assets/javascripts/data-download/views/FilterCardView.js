define(['backbone'], function(Backbone) {
  'use strict';
  var FilterCardView = Backbone.View.extend({

    initialize: function(options) {
      console.log(options);
    },
  });

  return FilterCardView;
});
